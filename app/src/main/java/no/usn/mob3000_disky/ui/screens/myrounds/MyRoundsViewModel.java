package no.usn.mob3000_disky.ui.screens.myrounds;

import androidx.compose.runtime.MutableState;

import java.util.ArrayList;
import java.util.List;

import javax.inject.Inject;

import dagger.hilt.android.lifecycle.HiltViewModel;
import no.usn.mob3000_disky.di.RepositoryModule;
import no.usn.mob3000_disky.model.Post;
import no.usn.mob3000_disky.model.User;

//@HiltViewModel
/*class MyRoundsViewModel(private val repository: RepositoryModule): ViewModel()) {
{
    val postList:MutableState<List<Post>>=mutableStateOf(ArrayList())
    val createPostResult:MutableState<Post>=mutableStateOf(Post(
            null,
                                                            User(0),"",
                0,
                null,
                "",
                ""


}

class MyProfileViewModel

        @Inject
        constructor(

        ):ViewModel(){

        val postList:MutableState<List<Post>>=mutableStateOf(ArrayList())

        val createPostResult:MutableState<Post> =mutableStateOf(Post(
        null,
        User(0),
        "",
        0,
        null,
        "",
        ""
        ))*/